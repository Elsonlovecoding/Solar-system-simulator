{
  "name": "orrery",
  "private": true,
  "description": "Interactive solar system simulator — real gravity, 29 bodies, 2D and 3D views",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-standalone.mjs",
    "start": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
